STARTING APPLICATION 

npm create vite@latest  then select options accordingly  FOR STARTING PROJECT 
THEN CREATE FOLDERS BACKEND AND FRONTEND AND ARRANGE FILE ACCORDINGLY 
if delete node_modules  FIRST GO TO ROOT FOLDER then run npm install 
npm run dev RUN THIS AFTER SHUTTING 

USE NEW TERMINAL CD TO BACKEND AND RUN PYTHON APP.PY

FOR DEPLOYMENT GENERALLY THIS IS STRUCTURE ROOT 
                                               FRONTEND- SRC FOLDER, INDEX.HTML, PACKAGE.JSON
                                               BACKEND- APP.PY,ROUTES.PY,MODELS.PY,REQUIREMENTS.TXT
                                               README.MD
                                               NODE_MODULES
                                               .GITIGNORE
RUN 
git add .
git commit -m "Add requirements.txt"
git push 
ON ROOT FOLDER TO PUSH ALL ON GITHUB THEN USE RENDER TO SELECT REPO
AND DEPLOY SEPARARTELY FRONTEND AS ROOT AND BACKEND AS ROOT (FIRST BACKEND THEN CHANGE ROUTES ADDRESS WITH REAL IT PROVIDES, IN APP.JSX THEN FRONTEND)
frontend
Build Command
Frontend/ $ npm install && npm run build
Publish Directory
Frontend/ dist
backend
Build Command
Backend/ $ pip install -r requirements.txt



